---
phase: 05-performance-and-seo
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/skeletons.test.ts
  - src/__tests__/image-loading.test.ts
  - src/__tests__/json-ld.test.ts
  - src/__tests__/sitemap.test.ts
  - tests/e2e/seo.spec.ts
autonomous: true
requirements: [PERF-02, PERF-03, SEO-01, SEO-02, SEO-03]

must_haves:
  truths:
    - "All 5 test stub files exist and are runnable (even if tests are skipped/pending)"
    - "npx vitest run completes without config errors"
  artifacts:
    - path: "src/__tests__/skeletons.test.ts"
      provides: "Test stubs for skeleton loading components (PERF-02)"
    - path: "src/__tests__/image-loading.test.ts"
      provides: "Test stubs for image lazy/eager loading (PERF-03)"
    - path: "src/__tests__/json-ld.test.ts"
      provides: "Test stubs for JSON-LD structured data (SEO-01)"
    - path: "src/__tests__/sitemap.test.ts"
      provides: "Test stubs for sitemap generation (SEO-02)"
    - path: "tests/e2e/seo.spec.ts"
      provides: "E2E test stubs for heading hierarchy and semantic HTML (SEO-03)"
  key_links: []
---

<objective>
Create test stub files for all Phase 5 verification points so that implementation plans can run their corresponding test commands.

Purpose: Satisfy Nyquist compliance by ensuring test files exist before implementation tasks reference them in <verify> blocks.
Output: 5 test stub files with pending/skipped tests describing expected behaviors.
</objective>

<execution_context>
@C:/Users/usr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/usr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-performance-and-seo/05-VALIDATION.md
@.planning/phases/05-performance-and-seo/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all 5 test stub files for Phase 5</name>
  <files>
    src/__tests__/skeletons.test.ts,
    src/__tests__/image-loading.test.ts,
    src/__tests__/json-ld.test.ts,
    src/__tests__/sitemap.test.ts,
    tests/e2e/seo.spec.ts
  </files>
  <action>
    Create 5 test stub files. Each file should use `describe` + `it.todo()` (vitest) or `test.describe` + `test.skip` (Playwright) to outline the expected test cases. The stubs must be syntactically valid and runnable (vitest will report them as "todo", Playwright as "skipped").

    1. **src/__tests__/skeletons.test.ts** (vitest + jsdom):
       ```typescript
       import { describe, it } from 'vitest'

       describe('skeleton loading components', () => {
         it.todo('PropertyCardSkeleton renders with correct structure')
         it.todo('PropertyListingSkeleton renders grid of card skeletons')
         it.todo('PropertyDetailSkeleton renders gallery and content placeholders')
         it.todo('loading.tsx for home page renders PropertyListingSkeleton')
         it.todo('loading.tsx for detail page renders PropertyDetailSkeleton')
       })
       ```

    2. **src/__tests__/image-loading.test.ts** (vitest + jsdom):
       ```typescript
       import { describe, it } from 'vitest'

       describe('image loading attributes', () => {
         it.todo('first gallery image has loading="eager"')
         it.todo('non-first gallery images have loading="lazy"')
         it.todo('property card images have loading="lazy"')
       })
       ```

    3. **src/__tests__/json-ld.test.ts** (vitest):
       ```typescript
       import { describe, it } from 'vitest'

       describe('JSON-LD structured data', () => {
         it.todo('buildPropertyJsonLd returns RealEstateListing schema')
         it.todo('price is raw number string, not formatted currency')
         it.todo('image URLs are absolute')
         it.todo('address fields map correctly')
         it.todo('optional fields are omitted when null')
       })
       ```

    4. **src/__tests__/sitemap.test.ts** (vitest):
       ```typescript
       import { describe, it } from 'vitest'

       describe('sitemap generation', () => {
         it.todo('sitemap includes home page entry')
         it.todo('sitemap includes all property URLs')
         it.todo('all URLs are absolute with base URL prefix')
         it.todo('property entries use updated_at as lastModified')
       })
       ```

    5. **tests/e2e/seo.spec.ts** (Playwright):
       Create the `tests/e2e/` directory if it does not exist. Use Playwright test syntax:
       ```typescript
       import { test } from '@playwright/test'

       test.describe('SEO and semantic HTML', () => {
         test.skip('each page has exactly one h1', async ({ page }) => {})
         test.skip('heading hierarchy does not skip levels', async ({ page }) => {})
         test.skip('property detail uses article and section elements', async ({ page }) => {})
         test.skip('property cards use article and figure elements', async ({ page }) => {})
       })
       ```
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/skeletons.test.ts src/__tests__/image-loading.test.ts src/__tests__/json-ld.test.ts src/__tests__/sitemap.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>All 5 test files exist, vitest reports todo tests without errors, Playwright file parses without syntax errors</done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/skeletons.test.ts` completes (todos reported)
- `npx vitest run src/__tests__/image-loading.test.ts` completes (todos reported)
- `npx vitest run src/__tests__/json-ld.test.ts` completes (todos reported)
- `npx vitest run src/__tests__/sitemap.test.ts` completes (todos reported)
- `tests/e2e/seo.spec.ts` file exists and is valid Playwright syntax
</verification>

<success_criteria>
- All 5 test stub files created and runnable
- vitest runs without config or import errors
- Wave 0 requirement from VALIDATION.md satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-performance-and-seo/05-00-SUMMARY.md`
</output>
