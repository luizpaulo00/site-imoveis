---
phase: 05-performance-and-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(public)/loading.tsx
  - src/app/(public)/imoveis/[id]/loading.tsx
  - src/components/public/skeletons/property-card-skeleton.tsx
  - src/components/public/skeletons/property-listing-skeleton.tsx
  - src/components/public/skeletons/property-detail-skeleton.tsx
  - src/components/public/property-card.tsx
  - src/components/public/property-gallery.tsx
  - src/components/public/property-detail.tsx
  - src/components/public/property-listing.tsx
  - src/components/public/header.tsx
  - src/app/(public)/page.tsx
autonomous: true
requirements: [PERF-01, PERF-02, PERF-03, PERF-04, SEO-03]

must_haves:
  truths:
    - "Public pages show skeleton placeholders immediately while data loads (no blank screen)"
    - "First visible image on each page is NOT lazy-loaded (eager for LCP)"
    - "Below-fold images use loading=lazy"
    - "All pages have exactly one h1 and proper heading hierarchy (h1 > h2 > h3)"
    - "Semantic HTML elements (article, section, figure, nav, main, footer) replace generic divs where appropriate"
  artifacts:
    - path: "src/app/(public)/loading.tsx"
      provides: "Home page skeleton fallback"
      min_lines: 5
    - path: "src/app/(public)/imoveis/[id]/loading.tsx"
      provides: "Detail page skeleton fallback"
      min_lines: 5
    - path: "src/components/public/skeletons/property-card-skeleton.tsx"
      provides: "Card skeleton matching real card layout"
      exports: ["PropertyCardSkeleton"]
    - path: "src/components/public/skeletons/property-listing-skeleton.tsx"
      provides: "Listing page skeleton with filter bar + card grid"
      exports: ["PropertyListingSkeleton"]
    - path: "src/components/public/skeletons/property-detail-skeleton.tsx"
      provides: "Detail page skeleton with gallery + content placeholders"
      exports: ["PropertyDetailSkeleton"]
  key_links:
    - from: "src/app/(public)/loading.tsx"
      to: "src/components/public/skeletons/property-listing-skeleton.tsx"
      via: "import PropertyListingSkeleton"
      pattern: "PropertyListingSkeleton"
    - from: "src/app/(public)/imoveis/[id]/loading.tsx"
      to: "src/components/public/skeletons/property-detail-skeleton.tsx"
      via: "import PropertyDetailSkeleton"
      pattern: "PropertyDetailSkeleton"
---

<objective>
Add skeleton loading states to all public pages, fix image lazy-loading for LCP optimization, and improve semantic HTML with proper heading hierarchy.

Purpose: Eliminate blank screens during data loading, optimize Core Web Vitals (LCP), and improve SEO through proper document semantics.
Output: Skeleton components, loading.tsx files, LCP-optimized image loading attributes, semantic HTML elements throughout public pages.
</objective>

<execution_context>
@C:/Users/usr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/usr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-performance-and-seo/05-RESEARCH.md

@src/components/ui/skeleton.tsx
@src/components/public/property-card.tsx
@src/components/public/property-detail.tsx
@src/components/public/property-listing.tsx
@src/components/public/property-gallery.tsx
@src/components/public/header.tsx
@src/app/(public)/page.tsx
@src/app/(public)/layout.tsx
@src/app/(public)/imoveis/[id]/page.tsx

<interfaces>
<!-- Skeleton base component already exists -->
From src/components/ui/skeleton.tsx:
```typescript
// shadcn Skeleton component with animate-pulse
export function Skeleton({ className, ...props }: React.HTMLAttributes<HTMLDivElement>)
```

From src/components/public/property-card.tsx:
```typescript
interface PropertyCardProps {
  property: PublicProperty
}
export function PropertyCard({ property }: PropertyCardProps)
// Card structure: aspect-[4/3] image -> p-4 body (price h-7, title h-4, neighborhood h-3, specs row h-4)
```

From src/components/public/property-detail.tsx:
```typescript
interface PropertyDetailProps {
  property: PropertyWithImages
  settings: PublicSettings
}
export function PropertyDetail({ property, settings }: PropertyDetailProps)
// Structure: gallery aspect-[16/9] -> mt-6 content (h1 title, price, specs, address, description, details table, map)
```

From src/components/public/property-listing.tsx:
```typescript
export function PropertyListing({ properties }: PropertyListingProps)
// Structure: PropertyFilters -> featured section grid -> regular section grid (cols-1 md:cols-2 lg:cols-3)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skeleton components and loading.tsx files</name>
  <files>
    src/components/public/skeletons/property-card-skeleton.tsx,
    src/components/public/skeletons/property-listing-skeleton.tsx,
    src/components/public/skeletons/property-detail-skeleton.tsx,
    src/app/(public)/loading.tsx,
    src/app/(public)/imoveis/[id]/loading.tsx
  </files>
  <action>
    Create three skeleton components that visually match the real component layouts to avoid CLS:

    1. **PropertyCardSkeleton** (`src/components/public/skeletons/property-card-skeleton.tsx`):
       - Import `Skeleton` from `@/components/ui/skeleton`
       - Match PropertyCard structure: `overflow-hidden rounded-xl bg-white shadow-sm ring-1 ring-gray-200` wrapper
       - `Skeleton` with `aspect-[4/3] w-full rounded-none` for image area
       - `div.p-4.space-y-2` body: `Skeleton h-7 w-32` (price), `Skeleton h-4 w-48` (title), `Skeleton h-3 w-24` (neighborhood), flex row with 3 `Skeleton h-4 w-12/w-12/w-16` (specs)

    2. **PropertyListingSkeleton** (`src/components/public/skeletons/property-listing-skeleton.tsx`):
       - Import `Skeleton` and `PropertyCardSkeleton`
       - Filter bar: `div.mb-6.flex.gap-3` with 3x `Skeleton h-10 w-32`
       - Card grid: `div.grid.grid-cols-1.gap-6.md:grid-cols-2.lg:grid-cols-3` with 6x `PropertyCardSkeleton`

    3. **PropertyDetailSkeleton** (`src/components/public/skeletons/property-detail-skeleton.tsx`):
       - `div.mx-auto.max-w-4xl` wrapper (matches PropertyDetail)
       - Gallery: `Skeleton aspect-[16/9] w-full rounded-lg`
       - Content: `div.mt-6.space-y-6.px-4.pb-8.md:px-0` with:
         - `Skeleton h-8 w-3/4` (title)
         - `Skeleton h-10 w-40` (price)
         - `div.flex.gap-6` with 4x `Skeleton h-6 w-20` (specs)
         - `Skeleton h-4 w-64` (address)
         - `div` section: `Skeleton h-6 w-24 mb-2` (heading) + 3x `Skeleton h-4 w-full` (description lines)
         - `Skeleton h-48 w-full rounded-lg` (map placeholder)

    4. **loading.tsx** for `(public)/`:
       - Import `PropertyListingSkeleton`
       - Return: `div` with `Skeleton h-9 w-48 mb-6` (h1 placeholder) + `PropertyListingSkeleton`

    5. **loading.tsx** for `(public)/imoveis/[id]/`:
       - Import `PropertyDetailSkeleton`
       - Return `PropertyDetailSkeleton`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All 5 files exist with correct exports, TypeScript compiles without errors, skeleton layouts match real component dimensions</done>
</task>

<task type="auto">
  <name>Task 2: Fix image loading attributes, semantic HTML, and heading hierarchy</name>
  <files>
    src/components/public/property-card.tsx,
    src/components/public/property-gallery.tsx,
    src/components/public/property-detail.tsx,
    src/components/public/property-listing.tsx,
    src/components/public/header.tsx,
    src/app/(public)/page.tsx
  </files>
  <action>
    **Image loading fixes (PERF-03, PERF-04):**

    1. In `property-gallery.tsx` line 72: Change the first image's `loading="lazy"` to `loading="eager"` for LCP. Keep all other slides as `loading="lazy"`. The simplest approach: in the `.map()`, check `index === 0` and set `loading="eager"` for index 0, `loading="lazy"` for others. Add the index parameter to the map callback.

    2. In `property-card.tsx` line 31: The cards already have `loading="lazy"` which is correct for most cards. No change needed here -- they are below-fold on the detail page. The listing page loads all data client-side so images appear after hydration regardless.

    **Semantic HTML (SEO-03):**

    3. In `property-card.tsx`: Change the outer `Link` wrapper -- add an inner `article` element inside the Link. Wrap the image `div` in a `figure` element. This maintains the Link as the clickable surface while adding semantic structure.
       - Current: `<Link ...><div>(image)</div><div>(body with h3)</div></Link>`
       - New: `<Link ...><article><figure>(image)</figure><div>(body with h3)</div></article></Link>`

    4. In `property-detail.tsx`: Wrap the entire content in `<article>` instead of `<div>`. Change the description wrapper `<div>` to `<section>`. Change the details table wrapper `<div>` to `<section>`. Change the map wrapper `<div>` to `<section>`. Heading hierarchy is already correct: h1 for title, h2 for section headings (Descricao, Detalhes, Localizacao).

    5. In `property-listing.tsx`: The `section` elements for featured and regular are already correct. No changes needed.

    6. In `header.tsx`: Verify the logo is NOT wrapped in h1 -- it currently uses just a `Link` with an `img`, which is correct. No changes needed.

    7. In `page.tsx` (home): The h1 is correct and there is only one. No changes needed.

    **Mobile-first audit (PERF-01):**
    Review existing Tailwind patterns -- the site already uses mobile-first responsive (`grid-cols-1 ... md:grid-cols-2 lg:grid-cols-3`). Verify touch targets in filter buttons are >= 44px (the h-10 = 40px, close enough with padding). No changes expected here -- the site was built mobile-first from Phase 4.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>First gallery image loads eagerly (LCP fix), all other images lazy, property cards wrapped in article/figure semantic elements, detail page uses article/section elements, one h1 per page, TypeScript compiles cleanly</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no type errors)
- `npx next build` succeeds
- Navigating to home page shows skeleton briefly before content (verifiable in slow 3G throttle)
- Navigating to property detail shows skeleton briefly before content
- View page source on detail page: article, section, figure elements present
- Only one h1 per page (home page h1 = site name, detail page h1 = property title)
- First gallery image does NOT have loading="lazy"
</verification>

<success_criteria>
- All public pages display skeleton loading states instead of blank screens
- LCP candidate image (gallery first slide) is eagerly loaded
- Semantic HTML elements (article, section, figure) used throughout public pages
- Heading hierarchy is h1 > h2 > h3 with no duplicates or skipped levels
- TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-performance-and-seo/05-01-SUMMARY.md`
</output>
