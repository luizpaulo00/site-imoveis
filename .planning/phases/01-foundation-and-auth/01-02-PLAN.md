---
phase: 01-foundation-and-auth
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/components/login-form.tsx
  - src/actions/auth.ts
  - src/app/admin/layout.tsx
  - src/components/admin/app-sidebar.tsx
  - src/components/admin/admin-topbar.tsx
  - src/app/admin/imoveis/page.tsx
  - src/__tests__/auth.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - "Admin can log in with email and password and is redirected to /admin/imoveis"
    - "Login page shows centered card with email/password fields, 'Entrar' button, and inline error feedback"
    - "Admin can log out from any admin page and is returned to /login"
    - "Admin layout shows a sidebar with navigation (Imoveis, Configuracoes) and logout button"
    - "Session persists across browser refresh (cookie-based, middleware refreshes token)"
  artifacts:
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Login page with centered card layout"
      min_lines: 10
    - path: "src/components/login-form.tsx"
      provides: "Login form with React Hook Form + Zod validation"
      min_lines: 40
    - path: "src/actions/auth.ts"
      provides: "signIn and signOut Server Actions"
      exports: ["signIn", "signOut"]
    - path: "src/app/admin/layout.tsx"
      provides: "Admin shell with SidebarProvider and sidebar"
      min_lines: 20
    - path: "src/components/admin/app-sidebar.tsx"
      provides: "Sidebar with nav items and logout"
      min_lines: 30
  key_links:
    - from: "src/components/login-form.tsx"
      to: "src/actions/auth.ts"
      via: "Server Action form submission"
      pattern: "signIn"
    - from: "src/actions/auth.ts"
      to: "@supabase/ssr"
      via: "signInWithPassword / signOut"
      pattern: "signInWithPassword|signOut"
    - from: "src/app/admin/layout.tsx"
      to: "src/components/admin/app-sidebar.tsx"
      via: "SidebarProvider + AppSidebar component"
      pattern: "AppSidebar"
---

<objective>
Build the login page, auth Server Actions, admin layout with sidebar, and logout functionality.

Purpose: The broker needs to authenticate and access a protected admin area with navigation. This plan delivers the complete auth flow (login -> admin shell -> logout) and the reusable admin layout that Phases 2-3 build upon.
Output: Working login page, signIn/signOut actions, admin layout with collapsible sidebar, placeholder imoveis page, and auth tests.
</objective>

<execution_context>
@C:/Users/usr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/usr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-auth/01-RESEARCH.md
@.planning/phases/01-foundation-and-auth/01-CONTEXT.md
@.planning/phases/01-foundation-and-auth/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- executor needs these contracts -->

From src/lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>
```

From src/lib/supabase/client.ts:
```typescript
export function createClient(): SupabaseClient
```

From src/lib/validations/settings.ts:
```typescript
export const settingsSchema: ZodObject<{whatsapp, site_name, broker_name}>
export type SettingsFormData = z.infer<typeof settingsSchema>
```

From src/middleware.ts:
- Redirects unauthenticated /admin/* to /login
- Redirects authenticated /login to /admin/imoveis
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create login page with auth Server Actions</name>
  <files>src/app/(auth)/login/page.tsx, src/components/login-form.tsx, src/actions/auth.ts, src/__tests__/auth.test.ts</files>
  <action>
1. Create `src/actions/auth.ts` with two Server Actions following EXACTLY the pattern from 01-RESEARCH.md Pattern 3:
   - `signIn(formData: FormData)`: Parse email+password with Zod (loginSchema: email string().email('Email invalido'), password string().min(6, 'Senha deve ter no minimo 6 caracteres')). Call `supabase.auth.signInWithPassword`. On error return `{ error: 'Email ou senha incorretos' }` (generic message -- do not reveal which field is wrong, per user decision). On success, `redirect('/admin/imoveis')`.
   - `signOut()`: Call `supabase.auth.signOut()`, then `redirect('/login')`.
   Both use `createClient` from `@/lib/supabase/server`.

2. Create `src/components/login-form.tsx` -- Client Component ("use client"):
   - Use React Hook Form with zodResolver for client-side validation
   - Login schema: email (required, valid email format), password (required, min 6 chars)
   - Fields: email Input with Label "Email", password Input with Label "Senha"
   - Button: "Entrar" -- shows spinner (Loader2 from lucide-react) + "Entrando..." text while submitting (use formState.isSubmitting or useTransition)
   - Error display: if server returns error, show it below the form in a subtle red text block: "Email ou senha incorretos"
   - On submit: call the signIn Server Action. Use startTransition to handle the server action call so the form stays interactive.
   - All labels, placeholders, and error messages in PT-BR

3. Create `src/app/(auth)/login/page.tsx`:
   - Layout: centered vertically and horizontally (min-h-screen flex items-center justify-center), background gray-50
   - Content: Card component from shadcn/ui containing:
     - Above the card or in CardHeader: site name text "Jander Imoveis" (or a generic "Admin" heading)
     - CardContent: LoginForm component
   - No "esqueci a senha" link (per user decision -- admin resets via Supabase dashboard)

4. Create `src/__tests__/auth.test.ts`:
   - Test: signIn with valid credentials calls signInWithPassword (mock Supabase client)
   - Test: signIn with invalid credentials returns error message "Email ou senha incorretos"
   - Test: signOut calls supabase.auth.signOut and redirects to /login
   - Mock `@/lib/supabase/server` createClient and `next/navigation` redirect

IMPORTANT: Use `signInWithPassword` (not `signInWithOtp` or `signUp`). Generic error message per security decision. Redirect to /admin/imoveis after login (per user decision).
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Login page renders centered card with email/password fields and "Entrar" button. signIn action authenticates via Supabase and redirects to /admin/imoveis. signOut action clears session and redirects to /login. Auth tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create admin layout with sidebar navigation and topbar</name>
  <files>src/app/admin/layout.tsx, src/components/admin/app-sidebar.tsx, src/components/admin/admin-topbar.tsx, src/app/admin/imoveis/page.tsx</files>
  <action>
1. Create `src/components/admin/app-sidebar.tsx` using shadcn/ui Sidebar component:
   - Use SidebarProvider pattern from shadcn/ui docs
   - Header: Logo/site name "Jander Imoveis" at the top of the sidebar
   - Navigation items (vertical, with icons from lucide-react):
     - "Imoveis" (Building2 icon) -> /admin/imoveis
     - "Configuracoes" (Settings icon) -> /admin/configuracoes
   - Footer: Logout button (LogOut icon) at the bottom of the sidebar. On click, call signOut Server Action from `@/actions/auth.ts`
   - Style: sidebar background in neutral dark tone (slate-900 or zinc-900 per user decision). Text in light color. Active nav item highlighted subtly. Clean, functional look -- no vibrant colors (per user decision: "Sem cores vibrantes no admin").
   - Responsive: on mobile/small screens, sidebar becomes a hamburger menu that opens a Sheet/Drawer (shadcn/ui Sidebar handles this automatically with SidebarProvider + SidebarTrigger)

2. Create `src/components/admin/admin-topbar.tsx`:
   - Displays current page title (accept as prop: `title: string`)
   - On mobile: includes SidebarTrigger (hamburger menu button) from shadcn/ui
   - Simple horizontal bar at the top of the content area
   - Style: white background, subtle bottom border, padding

3. Create `src/app/admin/layout.tsx`:
   - Server Component that wraps all /admin/* pages
   - Fetch current user with `createClient` from `@/lib/supabase/server` using `getUser()` -- if no user, this should not happen (middleware already redirects), but as a safety net redirect to /login
   - Render SidebarProvider > AppSidebar + main content area
   - The main content area should have proper padding and take remaining width
   - Include Toaster from sonner for toast notifications (used by settings page in Plan 03)

4. Create `src/app/admin/imoveis/page.tsx`:
   - Placeholder page for Phase 2
   - AdminTopbar with title "Imoveis"
   - Content: simple message "Nenhum imovel cadastrado." centered, with a subtle text indicating this will be built in the next phase
   - This page confirms the admin layout + sidebar work end-to-end

All text in PT-BR. Style follows user decisions: clean, white content area, dark neutral sidebar, no vibrant colors in admin.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Admin layout renders sidebar with "Imoveis" and "Configuracoes" nav items plus logout button. Sidebar collapses to hamburger on mobile. /admin/imoveis shows placeholder page with topbar. Build succeeds with no errors.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `npx vitest run --reporter=verbose` -- all tests pass (including auth tests)
3. Login page at /login shows centered card with form fields
4. After login, user sees admin layout with sidebar and imoveis placeholder
5. Logout button in sidebar returns user to /login
</verification>

<success_criteria>
- Login flow works: email/password -> signIn -> redirect to /admin/imoveis
- Logout flow works: click logout -> signOut -> redirect to /login
- Admin layout has sidebar (Imoveis, Configuracoes links, Logout button) with dark neutral theme
- Sidebar collapses to hamburger on mobile
- Session persists across refresh (middleware refreshes token cookie)
- All UI text in PT-BR
- Auth Server Actions tested
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-auth/01-02-SUMMARY.md`
</output>
